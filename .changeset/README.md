# Changesets

Run `pnpm changeset` for changes to `@snap-motion/core` or `@snap-motion/vue`. Applications,
fixtures, and documentation-only work do not require a Changeset. Packages remain private and the
repository has no automated publish command.
