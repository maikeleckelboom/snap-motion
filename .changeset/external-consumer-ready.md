---
"@snap-motion/core": minor
"@snap-motion/vue": minor
---

Close the external-consumer contract for the public beta candidate. Spatial surfaces now reconcile
controlled IDs and dynamic collections atomically, cancel pending pointer and wheel ownership before
authoritative changes, restore omitted physics options to product defaults, keep semantic
interactivity and focus aligned, and correlate compatibility-click suppression to the gesture that
earned it. Carousel navigation reasons are fixed by each public operation, keyboard defaults are
prevented only after accepted moves, and inherited direction updates content live while the physical
track remains left-to-right.

Package certification now installs real tarballs with lifecycle scripts disabled, proves an ordinary
Vue-only dependency graph plus a separate direct-core consumer, renders every Vue surface through
Vite and Nuxt SSR without client-only wrappers, preserves generic SFC inference at minimum and
current Vue, and exercises direction, keyboard, pointer, wheel, focus, and click behavior in a real
browser. Public type ownership, API Extractor diagnostics, generated chunk isolation, package
metadata, release evidence, and external integration documentation are hardened on the same terms.
